server:
  port: 9101
spring:
  application:
    name: myshop-user
  datasource:
    url: jdbc:mysql://106.52.237.153:3306/springcloud?characterEncoding=UTF8&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
  jpa:
    show-sql: true # ????sql??
    generate-ddl: true # ??????
    database: mysql # jpa????????
eureka:
  client:
    register-with-eureka: true # ??????????Eureka
    fetch-registry: true  # ????????Eureka??????
    service-url: # ???????
      defaultZone: http://localhost:8888/eureka
  instance:
  # ????????IP?????Eureka??????????true
  prefer-ip-address: true

ribbon:
  NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule

